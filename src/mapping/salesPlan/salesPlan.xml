<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="moka.salesPlan.dao.SalesPlanDao">

    <insert id="insert" parameterType="moka.salesPlan.bo.SalesPlan"
        useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_contract (
        buyComId, comContactsId, contractName, remarks, amt,
        userId,
        comId, signDate
        remark, paymentNum
        ) VALUES (
        #{buyComId},#{comContactsId},#{contractName},#{remarks},#{amt},#{userId},#{comId},#{signDate},
        #{remark},#{paymentNum}
        )
    </insert>


</mapper>